import { Meta } from '@storybook/addon-docs/blocks';

<Meta title='AppWrapper/Application Setup' />

# Application Setup

Through the use of AppWrapper, many out-of-the-box tools are available through a single component including pop-out menus on either side, a 3-section header, a 3-section footer, and optional included `ThemeWrapper`.
